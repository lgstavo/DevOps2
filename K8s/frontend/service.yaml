apiVersion: v1
kind: Service
metadata:
  name: redesocial-frontend
  labels:
    run: redesocial-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: redesocial-frontend